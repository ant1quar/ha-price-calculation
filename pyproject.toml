[project]
name = "ha-calc-addon"
version = "2.0.5"
description = "HACS custom integration + Lovelace card (dev/test metadata only)"
readme = "README.md"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
